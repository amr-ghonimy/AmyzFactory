@model IEnumerable<AmyzFactory.Models.InformationViewModel>


@if (Model != null && Model.Count() > 0)
{
    foreach (var item in Model)
    {
         <li class="mb-1"><a class="text-small reset-anchor">@item.Value : @item.Title</a></li>
    }
}
